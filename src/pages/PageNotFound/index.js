import React from 'react'
import { PageNotFound as PageNotFoundController } from '../../components/PageNotFound'

export const PageNotFound = (props) => {
  return <PageNotFoundController {...props} />
}

export default PageNotFound
